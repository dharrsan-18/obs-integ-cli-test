name: Build and Release Astra CLI

on:
  push:
    tags:
      - 'astra-cli-*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: linux
            arch: amd64
            ext: ""
          - os: linux
            arch: arm64
            ext: ""
          - os: windows
            arch: amd64
            ext: ".exe"
          - os: windows
            arch: arm64
            ext: ".exe"
          - os: darwin
            arch: amd64
            ext: ""
          - os: darwin
            arch: arm64
            ext: ""
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-go@v4
      with:
        go-version: '1.23.3'

    - name: Build
      env:
        GOOS: ${{ matrix.os }}
        GOARCH: ${{ matrix.arch }}
        CGO_ENABLED: 0
      run: |
        cd integrations/astra-cli
        go build -o apis-cli-${{ matrix.os }}-${{ matrix.arch }}${{ matrix.ext }}

    - name: Upload Artifact
      uses: actions/upload-artifact@v3
      with:
        name: binaries
        path: integrations/astra-cli/apis-cli-${{ matrix.os }}-${{ matrix.arch }}${{ matrix.ext }}

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - name: Download Artifacts
      uses: actions/download-artifact@v3
      with:
        name: binaries
        path: binaries

    - name: Create Version Release in Another Repository
      uses: softprops/action-gh-release@v1
      with:
        files: binaries/*
        tag_name: ${{ github.ref }}
        name: ${{ github.ref_name }}
        draft: false
        prerelease: false
        repository: getastra/astra-api-security-cli
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_REPO }}

    - name: Update Latest Release in Another Repository
      uses: softprops/action-gh-release@v1
      with:
        files: binaries/*
        tag_name: latest
        name: apis-cli-latest
        draft: false
        prerelease: false
        repository: getastra/astra-api-security-cli
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_REPO }}